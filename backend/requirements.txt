# ── Web / API (проверено на этапе 1) ───────────────────────
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-multipart==0.0.20
httpx==0.28.1

# ── LangChain (проверено на этапе 1) ───────────────────────
langchain==0.3.14
langchain-core==0.3.33
langchain-openai==0.2.14
langchain-ollama==0.2.3
langchain-text-splitters==0.3.5
# Оркестрация агента графом состояний. Диапазон, а не точный пин: API
# StateGraph стабилен внутри 0.2.x, а pip подберёт версию под langchain-core.
langgraph>=0.2.60,<0.3

# ── RAG / Vector store ─────────────────────────────────────
# chromadb НЕ пиним явно: langchain-chroma тесно завязан на конкретные
# версии chromadb, поэтому пусть pip подберёт совместимую сам.
langchain-chroma==0.2.0
chromadb

# ── Парсинг документов ─────────────────────────────────────
pypdf==5.1.0
python-docx==1.1.2

# ── Тесты ──────────────────────────────────────────────────
pytest==8.3.4

# ── База данных ────────────────────────────────────────────
sqlalchemy==2.0.36
psycopg2-binary==2.9.10     # синхронный драйвер Postgres (есть wheel под Windows)

# ── Очередь ────────────────────────────────────────────────
pika==1.3.2                 # клиент RabbitMQ (AMQP)
